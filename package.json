{
  "name": "portfolio",
  "private": true,
  "scripts": {
    "dev": "concurrently -n CLIENT,SERVER -c blue,green \"pnpm --dir frontend dev\" \"pnpm --dir backend dev\"",
    "dev:client": "pnpm --dir frontend dev",
    "dev:server": "pnpm --dir backend dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
